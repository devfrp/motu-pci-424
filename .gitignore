# Kernel module build artifacts
*.o
*.o.*
*.ko
*.mod
*.mod.c
*.mod.o
*.symvers
*.order
.*.cmd
.tmp_versions/
modules.order
Module.symvers

# Userspace tools
tools/motu424-probe
tools/motu424-ctl
__pycache__/

# Vendor installer (proprietary) — kept LOCAL only, never committed. Only the
# reverse-engineered facts extracted from it go into docs/, not the blobs.
MOTU Audio Installer*/

# Vendor binaries (proprietary, copyrighted) — kept LOCAL only, never committed.
# Only reverse-engineered facts (offsets, notes) go into the source, not blobs.
vendor/*
!vendor/README.md

# Editor / misc
*.swp
*~
